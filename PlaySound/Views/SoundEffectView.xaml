<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PlaySound.Views.SoundEffectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlaySound.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:PlaySound.Converters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:TimeSpanToSecondsConverter x:Key="TimeSpanToSecondsConverter" />
        <converters:SecondsToFormattedStringConverter x:Key="SecondsToFormattedStringConverter" />
    </UserControl.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="NormalState" />

                <VisualState x:Name="PlayingState">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind ViewModel.Effect.Playing, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PlayButton.Style" Value="{StaticResource DefaultButtonStyle}" />
                        <Setter Target="PlayButton.Command" Value="{x:Bind ViewModel.PauseCommand}" />
                        <Setter Target="PlayButton.(ToolTipService.ToolTip)" Value="Pause" />
                        <Setter Target="PlayButtonIcon.Glyph" Value="&#xE769;" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="0" Grid.ColumnSpan="5" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBox Background="Transparent" BorderThickness="0" Text="{x:Bind ViewModel.Effect.Name, Mode=TwoWay}" TextAlignment="Center" HorizontalAlignment="Stretch" Padding="6,7,6,6" />

            <Button Height="32" Width="32" Background="Transparent" BorderThickness="0" Padding="0" Command="{x:Bind ViewModel.RemoveCommand}" ToolTipService.ToolTip="Remove sound effect" Grid.Column="1">
                <FontIcon Glyph="&#xE74D;" FontSize="16" />
            </Button>
        </Grid>

        <Slider ThumbToolTipValueConverter="{StaticResource SecondsToFormattedStringConverter}" Minimum="0" Maximum="{x:Bind GetSeconds(ViewModel.Effect.Duration), Mode=OneWay}" StepFrequency="0.01" Height="130" Orientation="Vertical" Value="{x:Bind ViewModel.Effect.Position, Converter={StaticResource TimeSpanToSecondsConverter}, Mode=TwoWay}" HorizontalAlignment="Center" Margin="0,0,0,0" Grid.Row="1" />

        <ToggleButton CornerRadius="15" Width="30" Height="30" Padding="0" Margin="0,10,0,0" IsChecked="{x:Bind ViewModel.Effect.Loop, Mode=TwoWay}" VerticalAlignment="Top" HorizontalAlignment="Right" ToolTipService.ToolTip="Loop" Grid.Row="1" Grid.Column="1">
            <FontIcon Glyph="&#xE8EE;" FontSize="16" />
        </ToggleButton>

        <Button x:Name="PlayButton" CornerRadius="25" Width="50" Height="50" Margin="8,0" Command="{x:Bind ViewModel.PlayCommand}" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Top" HorizontalAlignment="Center" ToolTipService.ToolTip="Play" Grid.Column="2" Grid.Row="1">
            <FontIcon x:Name="PlayButtonIcon" Glyph="&#xF5B0;" FontSize="20" />
        </Button>

        <Button CornerRadius="15" Width="30" Height="30" Padding="0" Margin="0,10,0,0" Command="{x:Bind ViewModel.RestartCommand}" VerticalAlignment="Top" HorizontalAlignment="Left" ToolTipService.ToolTip="Restart" Grid.Column="3" Grid.Row="1">
            <FontIcon Glyph="&#xE72C;" FontSize="16" />
        </Button>

        <Slider Minimum="0" Maximum="1" StepFrequency="0.01" Height="130" Orientation="Vertical" Value="{x:Bind ViewModel.Effect.Volume, Mode=TwoWay}" HorizontalAlignment="Center" Grid.Row="1" Grid.Column="4" />
    </Grid>
</UserControl>
