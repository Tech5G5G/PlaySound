<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PlaySound.Views.ScenePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlaySound.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:PlaySound.Views"
    xmlns:models="using:PlaySound.Models"
    xmlns:contracts="using:PlaySound.Contracts"
    xmlns:viewmodels="using:PlaySound.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="EffectDataTemplate" x:DataType="viewmodels:SoundEffectViewModel">
            <views:SoundEffectView ViewModel="{x:Bind}" />
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox Text="{x:Bind ViewModel.Scene.Name, Mode=TwoWay}" FontSize="{StaticResource TitleLargeTextBlockFontSize}" FontWeight="SemiBold" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" TextWrapping="NoWrap" Padding="6" Margin="34,34,0,0" />

        <Button Grid.Column="1" Margin="24,40,40,0" ToolTipService.ToolTip="Add a sound effect (Ctrl+Alt+Shift+N)" Command="{x:Bind ViewModel.AddSoundEffectCommand}">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE710;" FontSize="16" />
                <TextBlock Text="Add sound effect" />
            </StackPanel>

            <Button.KeyboardAccelerators>
                <KeyboardAccelerator Modifiers="Control,Shift,Menu" Key="N" />
            </Button.KeyboardAccelerators>
        </Button>

        <GridView ItemsSource="{x:Bind ViewModel.Effects}"
                  ItemTemplate="{StaticResource EffectDataTemplate}"
                  SelectionMode="None"
                  Grid.ColumnSpan="2"
                  Grid.Row="1"
                  Padding="12,12,24,-4"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollMode="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollMode="Disabled">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </Grid>
</Page>
